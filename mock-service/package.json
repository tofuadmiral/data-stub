{
  "name": "mock-service",
  "version": "1.0.0",
  "description": "an app to mock services so tests can run in a stable fashion",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mountebank": "^2.1.0"
  }
}
